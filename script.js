let ken = 'ken';

let students = ['ken', 'izu', 'mic', 'tobs'];

for (let i = 0; i < students.length; i++) {
  // console.log(students.length)
  if (students[i].length == 3) {
    let second = students[i] + 4;
    console.log(second + students[i]);
  }
}

console.log(students);

let algebra = Math.random(10 * 10) + 1;

console.log(algebra);

let arrary = [1, 2, 3, 4, 5, 6, 7];

for (let i = 4; i < arrary.length; i++) {
  console.log(i);
}
